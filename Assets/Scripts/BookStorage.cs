using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BookStorage : MonoBehaviour
{
    public bool[] isFull;

    public GameObject[] slots;

    public int[] index;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
